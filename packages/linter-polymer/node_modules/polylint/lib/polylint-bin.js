#! /usr/bin/env node
'use strict';
const cli = require("./cli");
process.title = 'polylint';
cli.run(process.env, process.argv.slice(2), process.stdout)
    .then(function () {
    process.exit(0);
}, function () {
    process.exit(1);
});
